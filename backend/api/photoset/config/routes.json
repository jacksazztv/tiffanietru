{
  "routes": [
    {
      "method": "GET",
      "path": "/photosets",
      "handler": "photoset.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photosets/count",
      "handler": "photoset.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/photosets/:id",
      "handler": "photoset.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/photosets",
      "handler": "photoset.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/photosets/:id",
      "handler": "photoset.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/photosets/:id",
      "handler": "photoset.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
