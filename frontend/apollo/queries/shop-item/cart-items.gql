query CartItems($ids: [Int]!) {
  cartItems(ids: $ids) {
    id
    created_at
    updated_at
    name
    excerpt
    price
    subDiscount
    image {
      url
    }
    type
    slug
    customFields {
      id
      title
      required
      type
      placeholder
    }
  }
}